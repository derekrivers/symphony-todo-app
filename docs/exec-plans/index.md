# Execution Plans

This directory tracks active and completed plans for larger work.

- `active/` — in-flight execution plans.
- `completed/` — finished plans and decision logs.

Each plan should be executable, versioned, and linked to the associated implementation work.
