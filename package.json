{
  "name": "symphony-todo-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint . --ext .ts,.tsx",
    "test": "echo \"No tests yet\" && exit 0",
    "docs": "echo \"Generate docs\""
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "typescript": "^5.5.0"
  }
}
